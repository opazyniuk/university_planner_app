<%= form_with(model: shedule, local: true) do |form| %>
  <% if shedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shedule.errors.count, "error") %> prohibited this shedule from being saved:</h2>

      <ul>
      <% shedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day %>
    <%= form.text_field :day %>
  </div>

  <div class="field">
    <%= form.label :pair_number %>
    <%= form.number_field :pair_number %>
  </div>

  <div class="field">
    <%= form.label :type_of_lesson %>
    <%= form.text_field :type_of_lesson %>
  </div>

  <div class="field">
    <%= form.label :type_of_discipline %>
    <%= form.text_field :type_of_discipline %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
